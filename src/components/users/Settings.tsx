import React from "react";
import EditInfo from "./EditInfo";
import EditPassword from "./EditPassword";

type Props = {
  user: IUser;
  route: string;
};

export default function Settings({ user, route }: Props) {
  return <></>;
}
